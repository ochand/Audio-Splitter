# Bibliotecas de procesamiento de audio
librosa>=0.10.0
soundfile>=0.12.0
pydub>=0.25.0
numpy>=1.21.0

# Nuevas bibliotecas para formatos adicionales y metadatos
mutagen>=1.47.0          # Manejo de metadatos para MP3, FLAC, etc.
eyed3>=0.9.7             # Metadatos ID3 avanzados
tinytag>=1.10.0          # Lectura rápida de metadatos
ffmpeg-python>=0.2.0     # Conversión entre formatos

# Manejo de imágenes para carátulas
Pillow>=10.0.0           # Procesamiento de imágenes
requests>=2.28.0         # Para descargar carátulas online

# Interfaz de usuario
rich>=13.0.0             # Terminal UI mejorada
click>=8.1.0             # CLI framework avanzado
tabulate>=0.9.0          # Tablas en terminal

# Utilidades
tqdm>=4.64.0             # Barras de progreso
colorama>=0.4.6          # Colores en terminal
python-magic>=0.4.27     # Detección de tipos de archivo

# Testing y desarrollo
pytest>=7.0.0            # Framework de testing
pytest-cov>=4.0.0        # Cobertura de tests

# Calidad de código (opcional)
black>=22.0.0            # Formateador de código
flake8>=5.0.0            # Linter
mypy>=1.0.0              # Type checking
